<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    <%- include("../partials/header") %>
    <main>
    <div class="form__container">
        <h3 class="my-4">Registrarse</h3>
       <form class="row flex-column align-items-center g-3 needs-validation m-5" novalidate>
        
        <div class="input__container">
            <label for="name" class="form-label">Nombre</label>
            <input required name="name" id="name" type="text" class="form-control">
            <div class="invalid-feedback">Debes poner un nombre</div>
        </div>
        <div class="input__container">
            <label for="email" class="form-label">Email</label>
            <input required name="email" id="email" type="email" class="form-control">
            <div class="invalid-feedback">Es necesario poner un email valido</div>
        </div>
        <div class="input__container">
            <label for="password" class="form-label">Contraseña</label>
            <input required name="password" id="password" type="password" class="form-control">
            <div class="invalid-feedback">Es necesario poner una contraseña</div>
        </div>
        <div class="input__container">
            <label for="repassword" class="form-label">Contraseña</label>
            <input required name="repassword" id="repassword" type="password" class="form-control">
            <div class="invalid-feedback">Debes repetir contraseña</div>
        </div>
        <button type="submit" class="btn btn-outline-success text-black mt-5">Registrarme</button>
        </form> 
    </div>
    </main>
    <%- include("../partials/footer") %>
    <script>
                
        (() => {
        'use strict'
         const forms = document.querySelectorAll('.needs-validation')
            console.log(forms);
            Array.from(forms).forEach(form => {
            form.addEventListener('submit', event => {
            if (!form.checkValidity()) {
                event.preventDefault()
                event.stopPropagation()
            }

            form.classList.add('was-validated')
            }, false)
        })
        })()
    </script>
    <script src="/js/bootstrap.min.js"></script>
</body>
</html>